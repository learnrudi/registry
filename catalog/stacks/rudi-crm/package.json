{
  "name": "@rudi/rudi-crm-stack",
  "version": "0.1.0",
  "description": "RUDI CRM MCP stack for controlled engagement memory reads and writes.",
  "type": "module",
  "private": true,
  "main": "src/index.ts",
  "scripts": {
    "start": "npx tsx src/index.ts",
    "build": "tsc",
    "test": "npm run build && node --test tests/*.test.mjs",
    "test:live": "RUDI_CRM_LIVE_TESTS=1 npm test"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "pg": "^8.13.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@types/pg": "^8.11.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=20"
  }
}
